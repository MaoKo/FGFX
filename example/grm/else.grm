$EXTERN
{
	'else.lex.h',
};

$PRODUCTION
{
	<S> ->	'if' <E> 'then' <S>
		 |	'if' <E> 'then' <S> 'else' <S>
		 |	OTHER
		 ;

	<E> ->	NUM ;
};

$ALIAS
{
	(IF		=> 'if'),
	(THEN	=> 'then'),
	(ELSE	=> 'else'),
};
